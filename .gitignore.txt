# Extensiones de sistema (Mac y Windows)
.DS_Store
Thumbs.db
desktop.ini

# Carpetas de editores de código (VS Code, Sublime, etc.)
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# Dependencias (por si en el futuro usas npm)
node_modules/
dist/
build/

# Archivos de registro (Logs)
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Archivos con claves o datos sensibles (¡Muy importante!)
.env
.env.local
config.json

# Archivos temporales
*.tmp
*.bak
